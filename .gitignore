# Node modules and logs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Build output
dist/
admin/dist/
dist-ssr/
.vite/
*.tsbuildinfo

# Runtime / caches
.cache/
coverage/

# Environment files
.env
.env.*.local
*.local

# Firebase local emulator
.firebase/
firebase-debug.log

# OS generated files
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Images / uploads / backups that are generated at runtime
public/data/backups/
public/images/uploads/

# Temporary files
*.tmp
*.bak

# Optional locks (uncomment if you do NOT want lockfiles in repo)
# package-lock.json
# yarn.lock

# Keep the firebase.json and hosting config in repo, but ignore firebase hosting cache
.firebase/

# Other
coverage/

# Local firebase config (do not commit local project selection)
.firebaserc

# Local CSV backups and temp CSV files
public/data/*.bak
public/data/backups/

# Uploaded images (runtime)
public/images/uploads/
